A DCT (discrete cosine transform)-based denoising algorithm. Each channel is decomposed into local patches and then denoised by thresholding in the DCT domain, averaged and recombined.

![](screenshot.png)